# Field Reference

Each line contains one JSON object.

Required keys:

- id
- name_zh
- name_en
- publisher
- category
- license

Optional keys may record a source address, review date, descriptions, tags, status, reviewer, and replacement identifier.

Examples must use synthetic English text. Stable key names remain unchanged.
