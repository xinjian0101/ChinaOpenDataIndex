# Record Format

Each catalog line contains one JSON object.

Required keys: `id`, `name_zh`, `name_en`, `publisher`, `category`, and `license`.

Optional keys can record source URLs, review dates, descriptions, tags, status, reviewer, and replacement identifiers.

Public documentation and sample values are maintained in English. Stable key names remain unchanged for compatibility.
